# NEXT_PUBLIC_ASSET_PREFIX intentionally cleared — assets served from same origin as pages (www.tunee.ai).
# assetPrefix was removed from next.config.ts; publicAssetUrl() falls back to relative paths when this is unset.
