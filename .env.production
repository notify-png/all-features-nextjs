# public/ dir assets (workflow images etc) must also load from the Vercel deployment,
# because all-features-nextjs-new is reverse-proxied at tunee.ai/features/... only.
NEXT_PUBLIC_ASSET_PREFIX=https://all-features-nextjs-main.vercel.app
